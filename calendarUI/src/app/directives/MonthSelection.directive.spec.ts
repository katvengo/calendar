/* tslint:disable:no-unused-variable */

import { TestBed } from '@angular/core/testing';
import { MonthSelectionDirective } from './MonthSelection.directive';

describe('Directive: MonthSelection', () => {
  it('should create an instance', () => {
    const directive = new MonthSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
